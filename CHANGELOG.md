# 更新日志

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)。完整下载见 [Releases](https://github.com/humble26/desktop-workbench/releases)。

## v1.8.2

本次是**以修复与加固为主**的版本，重点解决了长期存在的数据一致性问题，并把项目结构重整为可测试的模块，补齐了回归测试。

### 修复（按修复清单顺序）

1. **主数据改为单一写者，修掉数据丢失隐患**
   此前渲染层每次改动都会把整份 `state` 写回文件，而主进程也会写同一个文件（全局快速添加待办、重复待办逾期顺延、到期提醒打标记、小组件开关）。两个写者 + 整份覆盖 = 后写者必然丢掉前者的改动。
   现在渲染层只提交「相对上次已知服务端状态的差异补丁」，由主进程应用到权威副本上，并发写入互不覆盖（核心回归用例已固化在 `test/storeproto.test.js`）。
2. **界面状态移出数据文件**
   当前页面、日历选择、待办筛选、番茄钟计时等不再写进 `workbench-data.json`；旧数据自动迁移（schema v1 → v2）：`_pomoDone→pomoDone`、`todos[]._remindedToday→remindedOn`、`settings._dailyRemindDate→dailyRemindOn`，**未知字段一律保留**，升级前自动留一份不会被轮转清理的备份。
3. **数据读取改为内存缓存 + 写合并**
   剪贴板轮询（1.2 秒）、时间统计采样（5 秒）、窗口拖动等高频路径不再反复读盘/写盘；渲染层的用户操作仍立即落盘。
4. **IPC 来源校验收紧**
   从「任意 `file://` 页面」收紧为「本应用 `renderer` 目录下的顶层框架」，并对文件类 IPC 强制绝对路径校验。
5. **PowerShell 环境显式化**
   启动即探测可用性、语言模式、`Add-Type` 与剪贴板 cmdlet 支持；`powershell.exe` 被策略禁用时自动回退 `pwsh.exe`；每个降级点记录原因并在「设置 · 运行环境 · 诊断」展示，取代过去的静默失败。
6. **代码结构重整**
   `main.js` 抽出 10 个 `lib/` 模块（数据仓库、迁移、默认值、来源校验、补丁校验、PowerShell、快速添加解析、时间统计、图标缓存、敏感内容）；`renderer/app.js`（2200 行）按原有章节边界拆成 20 个同目录脚本。拆分经过无损性校验（125 个顶层声明 → 125 个，无缺失、无重复）。
7. **自动文件整理规则即时生效**
   过去「已扫描过」的文件集合只增不减，新增/修改规则后此前扫过但不匹配的文件会被永久跳过；现在改为带时间窗的尝试记录，规则或监控目录变化即清空并重扫。
8. **回归测试**
   新增 119 项测试（协议、迁移、数据仓库、IPC 安全、快速添加解析、时间统计、图标缓存、补丁守卫、敏感内容、导入守卫、日期顺延、样式令牌、进程边界契约、主进程装配）+ 渲染层集成冒烟测试。

### 其他改进

- **图标改为落盘缓存**：快捷方式图标写到 `<userData>\icons\<sha1>.png`，数据文件里只留短引用，不再被 base64 撑大（几十个快捷方式可达数百 KB）。
- **设置页新增「运行环境 · 诊断」**：PowerShell 环境、数据仓库状态与修订号、时间统计采样、剪贴板记录、**全局快捷键注册结果**（被占用会明确提示）、图标缓存占用、降级中的功能清单。
- **提交补丁过守卫校验**：形状 / 体积 / 字段白名单，非法补丁被拒绝并回传原因，异常状态不会写坏数据。
- **非 Windows 平台不再注册无效的截图快捷键**；快捷键被其他程序占用时会提示。
- **深色主题令牌化**：原本靠 11 条补丁规则覆盖硬编码颜色，现改为在同一套令牌处给值，新增界面无需再写深色补丁（有样式测试防回退）。
- 使用说明补充「导出 / 备份的数据范围」（不含时间统计与剪贴板历史）与「检查更新不会自动下载安装」。

### 修复的具体问题（由新增测试发现）

- **PowerShell 探测被本地化警告破坏**：`Add-Type` 一个空类会在中文系统打印 `警告: 所生成的类型未定义公共方法或属性。`，混进 stdout 后 JSON 解析必然失败，导致中文 Windows 用户被误判为「PowerShell 不可用」，时间统计与剪贴板文件功能被静默关闭。现改为容忍噪音的解析（多行 / BOM / 警告 / 格式化 JSON），并且探测类带公共成员。
- **敏感内容过滤漏检与误判**（合并上游实现时发现并修正）：以中文开头的 `登录口令：xxx` 因 `\b` 词边界不成立而漏检；`github_pat_` 细粒度 Token 未覆盖；`const token = getToken();` 这类代码被误判为凭据（会误伤正常复制内容）。
- **默认数据结构未归一化**：每次全新启动都被当成一次结构升级（重写数据文件并生成迁移前备份）。
- **首次运行不建立数据文件**、**提交走写合并导致 150ms 内的崩溃会丢改动**（渲染层提交已改为立即落盘）。

### 升级说明

- 覆盖安装即可，用户数据与设置保留；首次启动会执行一次结构迁移并留下 `workbench-premigrate-*.json` 备份。
- 如果你的快捷方式图标在升级后短暂显示为字母头像，是旧的内联 base64 图标被清理后重新提取所致（提取成功后会写入图标缓存）。

## v1.8.1

- 修复：部分快捷方式（如 ZCode）图标显示为通用占位图的问题 —— 图标提取增加 GDI+ `ExtractAssociatedIcon` 兜底并内置占位图检测；解析失败时显示字母头像。
- 安全 / 稳定性加固：数据导入结构校验、剪贴板敏感内容过滤（JWT / 私钥 / 口令 / API Key 等不录入历史）、截图裁剪边界与大小限制、月末重复任务日期安全顺延、自动整理失败重试、保存失败提示。

## v1.8.0

- 截图 OCR 取字（Win+Alt+S）：框选屏幕任意区域，离线识别文字并自动复制。
- 命令面板（Ctrl+K 升级）：搜索内容之外可直接执行动作 —— 新建待办/便签、打开剪贴板、截图取字、开始番茄钟、切换置顶/主题、立即备份、快速跳转页面。
- 桌面小组件：时钟 / 今日待办 / 便签 可固定为桌面小窗，位置自动记忆。

## v1.7.0

- 自动时间统计（仅 Windows）：后台每 5 秒采样前台应用，记录实际使用时长。
- 分类规则自动归类（工作 / 开发 / 浏览 / 沟通 / 娱乐 / 其他），规则可增删改，改完对历史数据即时生效。
- 新增「时间统计」页：今日时长与增减、分类占比、应用 Top10、近 7 天趋势、番茄钟标注。
- 数据洞察页新增「今日活跃时长 / 最常用应用」。
- 空闲超阈值自动暂停（2 / 5 / 10 分钟可调），锁屏立即暂停；窗口标题记录默认关闭。
- 统计数据独立存放 `usage-data.json`（自动保留 90 天），不参与导出 / 备份，可单独清空。

## v1.6.2

- 设置页重构：按「窗口·外观 / 功能 / 数据」分组，行内加图标锚点，窄窗口下描述文字自动换行。
- 修复：部分 `.lnk` 快捷方式添加后只显示通用占位图标的问题。

## v1.6.0

- 剪贴板历史：全局快捷键 Win+Alt+V 唤出，自动记录 文本 / 图片 / 文件。
- 支持搜索、置顶、按类型过滤；点击即重新复制（文件可整组写回）。
- 图片条目内置 OCR 提字（tesseract.js + chi_sim/eng 离线模型，无需联网）。
- 设置中可一键关闭剪贴板历史（关闭后停止记录并注销快捷键）。

## v1.5.0

- 数据洞察页（完成率、近 14 天活动强度、当前情况一览）。
- 番茄钟（专注 / 短休 / 长休）。
- 全局快速添加（Win+Alt+T，支持「今天 / 明天 / 15:30」等快捷写法）。
- 重复待办逾期自动顺延（设置中开启）。
- 毛玻璃背景（Windows 11 覆盖桌面模式下）。
- 自动文件整理规则（监控文件夹，按扩展名 / 关键词自动归类）。
- 内置检查更新（设置中填写更新源地址即可使用）。
